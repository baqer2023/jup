enum LanguageEnum {
  ENGLISH,
  PERSIAN,
}

enum RequestMethodEnum {
  GET,
  POST,
  DELETE,
  PUT,
  PATH,
}

enum GetDashboardsSortProperty {
  createdTime,
  title,
}

enum GetDevicesSortProperty {
  createdTime,
  name,
  deviceProfileName,
  label,
  customerTitle,
}

enum sortOrder {
  ASC,
  DESC,
}

enum scope { SHARED_SCOPE, SERVER_SCOPE }
